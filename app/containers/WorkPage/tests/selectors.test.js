// import { fromJS } from 'immutable';
// import { makeSelectWorkPageDomain } from '../selectors';

// const selector = makeSelectWorkPageDomain();

describe('makeSelectWorkPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
